<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:local="clr-namespace:AppClass"
             x:Class="AppClass.MyPage6"
             BindingContext="{local:Man}">
    <ContentPage.Content>
        <StackLayout Margin="0,44,0,0">
            <!--<Label x:Name="lblName" Text="{Binding Name}"/>
            <Label x:Name="lblAge" Text="{Binding Age}"/>-->
            
            <Entry Text="{Binding Name}"/>
            <Entry Text="{Binding Age}"/>
            <!--<Button Text="Button" BackgroundColor="Silver" Command="{Binding OnAddAge}"/>-->
            <Button Text="Button" BackgroundColor="Silver" Clicked="Handle_Clicked"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
